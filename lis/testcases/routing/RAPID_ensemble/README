RAPID Ensemble Testcase

This is a testcase that uses:
  (a) MOGREPS-G forecast forcing
  (b) The Noah version 3.9 land surface model
  (c) Global (557WW) 10 km domain
  (d) 18 ensemble members 
  (e) A time period from 0Z 1 Dec 2023 to 00Z 9 Dec 2023

This directory contains:
  (a) This README file
  (b) The "ldt.config" file used to generate the parameters using LDT
  (c) The "lis.config" file used for this testcase
  (d) The "MODEL_OUTPUT_LIST.TBL" file used by the lis.config file
      to select the output variables

Note that the "ldt.config" and "lis.config" files should be edited
to make sure that the locations of the parameter and forcing files
are specified correctly.

To run this testcase:
  (a) Generate the LDT and the LIS executables
  (b) Run the LDT executable using the "ldt.config" file
      with the testcase input data
  (c) Run the LIS executable using the "lis.config" file with the
      testcase input data

Caveats:
  (a) Please note that this is a simple functional test and the output
      from the testcase is not expected to be used for any scientific
      evaluation.
  (b) This does not produce any surface module output, only RAPID router output.
